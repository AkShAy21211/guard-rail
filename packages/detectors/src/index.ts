/**
 * @guardrail/detectors — Phase 1 placeholder.
 * Phase 4 implements the individual detectors (typescript, framework, filesystem, naming, dependencies).
 */
export const DETECTORS_PLACEHOLDER = true;
